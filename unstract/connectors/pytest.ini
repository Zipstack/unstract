[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v -s --tb=short

# Configure test markers
markers =
    unit: Unit tests
    integration: Integration tests
    email: Tests for email connectors
