{
    "title": "SFTP",
    "description": "A connector that allows for fetching data via SFTP",
    "type": "object",
    "required": [
      "connectorName",
      "host"
    ],
    "properties": {
      "connectorName": {
        "type": "string",
        "title": "Name of the connector",
        "default": "Unstract's SFTP connector"
      },
      "host": {
        "type": "string",
        "title": "Host/IP",
        "description": "The hostname or IP address of the SFTP server. Example: `sftp.example.com` or `192.168.1.10`."
      },
      "port": {
        "type": "string",
        "title": "Port",
        "description": "The port number to connect to the SFTP server. Example: `22` (default SSH port).",
        "default": "22"
      },
      "username": {
        "type": "string",
        "title": "Username",
        "description": "The username used to authenticate with the SFTP server"
      },
      "password": {
        "type": "string",
        "title": "Password",
        "description": "The password used to authenticate with the SFTP server",
        "format": "password"
      },
      "directory": {
        "type": "string",
        "title": "Directory",
        "description": "The directory user has accesed to"
      }
    }
  }
