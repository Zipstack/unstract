{
  "title": "Oracle DB database",
  "description": "Designed with a patented new architecture to handle all aspects of data and analytics",
  "type": "object",
  "required": [
    "connectorName",
    "config_dir",
    "user",
    "password",
    "dsn",
    "wallet_location",
    "wallet_password"
  ],
  "properties": {
    "connectorName": {
      "type": "string",
      "title": "Name of the connector",
      "default": "Unstract's Oracle DB connector"
    },
    "config_dir": {
      "type": "string",
      "title": "Specifies the directory containing tnsnames.ora ",
      "default": "/opt/OracleCloud/MYDB"
    },
    "user": {
      "type": "string",
      "title": "Specifies the database user ",
      "default": "admin"
    },
    "password": {
      "type": "string",
      "title": "Specifies the database user password ",
      "format": "password",
      "default": ""
    },
    "dsn": {
      "type": "string",
      "title": "Specify the desired network or the TNS names",
      "default": "db2024_low"
    },
    "wallet_location": {
      "type": "string",
      "title": "Specifies the directory containing the PEM file (ewallet.pem)",
      "default": "/opt/OracleCloud/MYDB"
    },
    "wallet_password": {
      "type": "string",
      "title": "Specifies the password for the PEM file (ewallet.pem). You set this password when you download the wallet.zip file ",
      "format": "password",
      "default": ""
    }
  }
}
