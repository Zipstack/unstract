"""
Connector __init__.py Template

Replace placeholders:
- {ClassName}: PascalCase class name (e.g., PostgreSQL, MinioFS)
- {connector_name}: lowercase module name (e.g., postgresql, minio)
- {description}: Brief description of the connector
"""

from .{connector_name} import {ClassName}

metadata = {
    "name": {ClassName}.__name__,
    "version": "1.0.0",
    "connector": {ClassName},
    "description": "{description}",
    "is_active": True,
}
