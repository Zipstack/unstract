{
  "schemaVersion": "0.0.1",
  "displayName": "OCR",
  "functionName": "ocr",
  "toolVersion": "0.0.1",
  "description": "This is tool which can be used to extract text from images. Perform OCR operations on images.",
  "input": {
      "description": "The file on which OCR operation is to be performed"
  },
  "output": {
      "description": "Places the file into a folder (bin) that its OCR opertation performed into."
  },
  "result": {
      "type": "JSON",
      "description": "JSON response containing the bin to which the file was classified into",
      "schema": {}
  },
  "adapter": {
      "ocrs": [
          {
              "isEnabled": true,
              "adapterId": "ocrAdapterId",
              "title": "OCR to use",
              "isRequired": true,
              "description": "OCR used to extract text from images"
          }
      ]
  },
  "ioCompatibility": {
      "api": {
          "sourceSupport": true,
          "destinationSupport": true,
          "additionalArgs": {
              "sync": true
          }
      },
      "file": {
          "sourceSupport": true,
          "destinationSupport": true,
          "additionalArgs": {}
      },
      "db": {
          "destinationSupport": true,
          "additionalArgs": {}
      }
  },
  "restrictions": {
      "maxFileSize": "10MB",
      "allowedFileTypes": [
        "jpeg",
        "png",
        "tiff",
        "bmp",
        "gif",
        "webp",
        "pdf"
      ]
    }
}
