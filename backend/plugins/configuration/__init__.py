"""Cloud configuration plugin initialization."""

# Metadata for the configuration plugin
metadata = {
    "name": "CloudConfiguration",
    "is_active": True,
    "description": "Cloud-specific configuration extensions",
}